# Phase1 システム実装計画

## 0. 前提整備

## 0.1 作成依頼者への確認質問
1. ワークブックの想定利用者と利用シナリオ（部署・利用頻度・利用時間帯）は？
   - 回答: 週1回、利用者は1名（依頼者ご本人）。
2. シート保護の要件（入力可能セル・保護パスワード有無・保護解除フロー）は？
   - 回答: シート保護は不要。
3. マクロ署名について、利用可能な証明書や署名ポリシー、必要なタイミングは？
   - 回答: マクロ署名は不要。
4. バージョン情報セルに記載すべきフォーマット（例: バージョン番号／リリース日／担当者）は？
   - 回答: バージョン番号・リリース日・担当者・変更点メモ（20文字程度）。
5. 出力ファイルの保存先（共有ドライブや Teams など）と命名規則の最終確認は？
   - 回答: 指定なし。既定のままで問題なし。
6. 参照ライブラリ以外に追加で必要なアドイン／外部接続先はあるか？
   - 回答: 追加不要。
7. マスターデータの管理方法（外部ファイル連携・ワークブック内完結など）と更新フローは？
   - 回答: 現時点では取り決めなし（未定）。
8. ログの保管期間・匿名化ルール・閲覧権限はどうするか？
   - 回答: 特段の取り決め不要。
9. 運用・保守担当とエスカレーション経路（連絡先・対応時間）は？
   - 回答: 特段の取り決め不要。
10. リリーススケジュール、テスト期間、受入条件（合格基準）は？
    - 回答: 特段の取り決め不要。

## 0.2 依頼者回答を反映した実施チェックリスト
- [x] シート保護方針に基づく保護設定の実装（不要と確認済み）
- [x] マクロ署名手順および証明書の組み込み（不要と確認済み）
- [x] バージョン情報セル／履歴領域のフォーマット確定と実装
- [ ] 環境別出力パス切り替え設定（共有ドライブ等）の方針決定
- [ ] マスターデータ管理フロー（外部ファイル同期 or 手動更新）実装
- [ ] ログ保持ポリシーに合わせた自動ローテーション処理
- [ ] 受入テスト項目と結果記録テンプレートの作成
- [ ] リリース通知／研修資料のドラフト作成
- [x] 開発端末の要件確認（Windows 10/11・Excel 2016 以上・PowerShell 5.1）
- [ ] `C:\VBA-Automation` への配置とリポジトリ複製手順を整備
- [ ] Excel マクロ有効化と信頼済み場所登録を全担当者向けにマニュアル化
- [ ] PowerShell 実行ポリシー設定 (`RemoteSigned`) の周知とスクリプト署名方針の合意
- [ ] `config/build-config.json` の参照設定と出力命名規約の凍結

## 1. ビルド基盤
- [x] `tools/Build-VbaProject.ps1` の機能拡充（設定読込・テンプレ複製・VBA インポート）
- [x] ログ出力（成功/失敗・経過時間・参照設定結果）の構造化（JSON + テキスト）
- [x] 失敗時のクリーンアップ処理（中間ファイル削除・Excel プロセス終了）
- [x] 参照ライブラリ自動追加処理の実装とリトライ戦略定義
- [x] `build.bat` からの呼び出しフロー確立（引数受渡し・終了コード伝播）

## 2. Excel テンプレート
- [x] `template/BaseTemplate.xltm` のワークシート構成（入力・集計・マスタ）定義
- [x] 必要な名前定義・テーブル・初期データの反映（ビルド時に自動更新）
- [x] UI コントロール配置（ボタン・フォーム）とイベントひな型実装
- [ ] 保護設定（シート保護・マクロ署名）の方式設計
- [x] バージョン情報セルと履歴記録領域の設置

## 3. VBA モジュール実装
- [x] `MainModule`: エントリーポイント・起動時初期化シーケンス実装
- [x] `Utils`: 共通関数（Null 安全・数値変換・文字列処理）の整備
- [x] `DataManager`: データ読込/保存・検証・エラーハンドリング実装
- [x] エラーログ/メッセージ表示ラッパー標準化 (`Logger` モジュール)
- [x] ユーザー操作ログ（開始/完了/異常）の記録処理実装

## 4. 設定・データ定義
- [x] `config/build-config.json` の構成項目確定（テンプレ/ソース/出力/参照/ログ）
- [ ] 環境別設定差分の取り扱い（例: 共有ドライブ出力）の方針決定
- [ ] マスターデータの管理方法（外部ファイル vs 埋込）合意
- [ ] 設定値検証用スキーマ/チェック処理の実装

## 5. 品質保証
- [ ] PowerShell 単体テスト（Pester 等）でビルドスクリプト検証
- [ ] Excel マクロのスモークテスト手順（手動チェックリスト）策定
- [ ] 自動エクスポート後のファイル差分検証フロー（Git 連携）設計
- [ ] 主要シナリオのユーザー受入テスト実施と記録

## 6. 運用準備
- [ ] ビルド実行ガイド更新（スクリーンショット・トラブルシュート）
- [ ] `logs/` 保守方針（保持期間・匿名化）策定
- [ ] リリースパッケージ構成（.xlsm・ログ・README）確定
- [ ] バックアップ/復旧手順（テンプレ・出力）の整備
- [ ] 保守問い合わせフローと責任分界の整理

## 7. 移行と展開
- [ ] 既存 Excel 資産からのデータ移行手順定義
- [ ] 初回リリーススケジュールと関係者通知テンプレ作成
- [ ] 利用者トレーニング（資料・ハンズオン）の実施
- [ ] ロールアウト後のフォローアップミーティング計画

---
進捗更新時は該当チェックボックスを `- [x]` へ変更する。